import 'package:flutter/material.dart';

class ManualInputTab extends StatelessWidget {
  const ManualInputTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Manual input form will be added here'),
    );
  }
}
